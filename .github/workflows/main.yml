name: Scheduled Tasks
on:
  schedule:
    - cron: '*/5 * * * *'

jobs:
  update-price-data:
    runs-on: ubuntu-latest
    steps:
    - name: Update Price Data
      run: curl -X POST https://pascal.fi/api/updatePriceData
